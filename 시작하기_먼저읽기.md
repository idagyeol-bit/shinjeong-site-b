# 신정개발 홈페이지 — Claude 코딩 프로그램에서 바로 시작하기

이 압축 파일을 풀면 `shinjeong-site-b` 폴더가 생깁니다. 이 폴더가 홈페이지 전체이며, GitHub 저장소(`idagyeol-bit/shinjeong-site-b`)와 똑같은 내용(커밋 `7de2f6a`)입니다. 설치할 것은 없습니다.

## 1. 바로 보기 (10초)
- 폴더 안의 `index.html` 을 더블클릭하면 브라우저에서 열립니다. (이 방법은 글꼴만 기본 글꼴로 보입니다. 정상입니다.)
- 실제 사이트와 똑같이 보려면: 폴더에서 PowerShell을 열고
  `powershell -ExecutionPolicy Bypass -File serve.ps1`
  를 실행한 뒤 브라우저에서 `http://localhost:4173/` 을 엽니다.

## 2. Claude 코딩 프로그램(Claude Code)에서 열기
1. Claude Code(터미널) 또는 VS Code의 Claude 확장에서 **이 폴더(`shinjeong-site-b`)를 엽니다.**
2. 폴더 안의 `CLAUDE.md` 를 Claude가 자동으로 읽어 작업 규칙(사실만, 사진은 ZIP만, build.js 수정 후 재생성)을 지킵니다.
3. 이렇게 말하면 됩니다.
   - `사이트를 미리보기 서버로 띄워 줘`
   - `tools/build.js 의 C.tel 을 061-000-0000 으로 바꾸고 다시 생성해 줘`
   - `현장 진행 방식 페이지 STEP 03 설명을 "…"로 바꿔 줘`
   - `assets/photos 에 새 사진을 W11 로 넣고 설비 클리닝 '장비와 현장'에 추가해 줘`
   - `변경 사항을 커밋하고 GitHub에 푸시해 줘` → 1~2분 뒤 https://idagyeol-bit.github.io/shinjeong-site-b/ 에 반영

## 3. 꼭 기억할 것
- **`tools/build.js` 가 원본**입니다. HTML을 직접 고치면 다음 `node tools/build.js` 때 덮어써집니다.
- 글은 회사소개서·기술소개서에 있는 사실만, 사진은 사진 ZIP 3개에서만 씁니다. (자세한 규칙: `CLAUDE.md`, 사용법: `README.md`)
- 아직 클라이언트 확인이 필요한 것: 로고 원본, 현 대표자명, 발주처명 공개 범위, 인증서 스캔, 도메인(sjdevel.com) 연결, 문의 서버.

## 4. 폴더 구성
| 파일/폴더 | 내용 |
| --- | --- |
| `index.html` … `process.html` (13개) | 페이지 (생성 결과물) |
| `tools/build.js` | 모든 내용의 원본 데이터와 생성기 |
| `tools/photo-plan.json` | 사진 ID ↔ 원본 ZIP 파일 ↔ 크롭 좌표 |
| `assets/photos/` | 사진 24장 × 크기별 파일 |
| `assets/fonts/` | Pretendard 글꼴(OFL 라이선스 동봉) |
| `css/`, `js/` | 스타일과 스크립트 (외부 라이브러리 없음) |
| `README.md` | 운영 설명서 (고치는 법, 올리는 법, 체크리스트) |
| `CLAUDE.md` | Claude Code 작업 규칙 |
| `.git/` | GitHub 저장소 연결 정보 (지우지 마세요 — 푸시에 필요) |
